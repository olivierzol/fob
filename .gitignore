# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# macOS
.DS_Store

# Xcode (in case an app target is added later)
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
DerivedData/

# fob runtime data never lives in-tree (it's in ~/.fob), but guard anyway
*.sock
*.policy
audit.log
audit.head.sig
